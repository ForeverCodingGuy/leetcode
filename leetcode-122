class Solution {
public:
    int maxProfit(vector<int>& prices) 
    {
        int rsu = 0;
        for(int i = 1;i < prices.size();i++)
        {
            rsu += max(prices[i] - prices[i - 1],0);
        }

        return rsu;
    }
};
